import SwiftUI

enum Theme {
    static let background = Color(hex: 0xFBF8EF)
    static let cardBackground = Color(hex: 0xFFFFFF)
    static let primary = Color(hex: 0x2F80ED)
    static let success = Color(hex: 0x27AE60)
    static let warning = Color(hex: 0xEBC344)
    static let danger = Color(hex: 0xE74C3C)
    static let muted = Color(hex: 0x8C8C8C)
}
